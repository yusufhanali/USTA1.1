/**:
  ros__parameters:

    align_depth.enable: false

    camera_namespace: "hri"
    camera_name: "overhead"
    base_frame_id: "hri_camera_link"

    device_type: "d455"

    rgb_camera.profile: "1280x720x30"
    depth_module.profile: "1280x720x30"

    enable_accel: false
    enable_color: true
    enable_depth: true
    enable_gyro: false
    enable_infra1: false
    enable_infra2: false
    enable_rgbd: false
    enable_sync: true
    
    pointcloud.enable: true
    pointcloud.stream_filter: 2 # 2 = COLOR stream

    serial_no: "313522301714"
